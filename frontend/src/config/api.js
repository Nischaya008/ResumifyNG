const API_BASE_URL = 'https://resumifyng-backend.up.railway.app/api'

export const endpoints = {
  uploadResume: `${API_BASE_URL}/upload_resume`,
  // Add other endpoints as needed
}

export default API_BASE_URL